apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tradestore
  namespace: freqtrade
  # annotations:
  #   volume.beta.kubernetes.io/storage-class: "nfs-client"
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi

